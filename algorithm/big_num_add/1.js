/**
 * 
 * @param {string} num1 
 * @param {string} num2 
 */
function addLargeNumbers(num1, num2) {
   let result = ''; // 用于存储结果的字符串
   let carry = 0; // 进位
   let i = num1.length - 1; // num1 的指针
   let j = num2.length - 1; // num2 的指针
}